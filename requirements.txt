# --- core ---
numpy>=1.24
scipy>=1.10
Pillow>=10.0
opencv-python-headless>=4.8

# --- depth backbone (frozen, zero-shot) ---
torch>=2.0
transformers>=4.40
# Default is Depth-Anything-V2-Large (~1.3 GB, 8-10x slower on CPU than Small).
# Override without editing code:  export DEPTH_MODEL=depth-anything/Depth-Anything-V2-Small-hf

# --- geospatial ---
rasterio>=1.3
requests>=2.31

# --- mesh ---
trimesh>=4.0

# --- API (Gradio is gone; the front end is React in web/) ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# --- validation figures ---
matplotlib>=3.7
